version: 0.4.0
targets:
  seeedstudio-xiao-esp32c6:
    chip: esp32c6fx4
    flags:
      - has_usb_cdc_acm_port
    leds:
      # led0
      - pin: GPIO15
        color: yellow
        active: low
        aliases:
          - D4
          - USER_LED
    buttons:
      # button0
      - pin: GPIO9
        active: low
        aliases:
          - BOOT
    uarts:
      - rx_pin: GPIO17
        tx_pin: GPIO16
        possible_peripherals:
          - UART0
          - UART1
        host_facing: true
