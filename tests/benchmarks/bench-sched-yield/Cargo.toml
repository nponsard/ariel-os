[package]
name = "bench-sched-yield"
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
ariel-os = { workspace = true, default-features = true, features = [
  "bench",
  "threading",
] }
ariel-os-boards = { workspace = true }

[lints]
workspace = true
